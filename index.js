const con='chapter one paragraph one it all start when with the lonelyness..there was nobody on myside';
function startRiding(){
	var text=document.getElementById("abc").innerHTML;
		if(text===con){
			document.getElementById("abc").innerHTML = "Hell";
		}else{
			document.getElementById("abc").innerHTML = con;
		}
	
}
